# first-ai-model
Simple AI model which recognizes images using PyTorch.

## Setup Instructions

1. Clone this repository.
2. Download the CIFAR-10 dataset archive (`cifar-10-python.tar.gz`).
3. Place the extracted archive inside a folder named `data_cifar/` in the root directory before running the model.